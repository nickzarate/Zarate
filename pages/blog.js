import Layout from '../components/Layout'
import { home } from '../assets/colors'

const Blog = () => (
  <Layout>
    <h1 style={{ backgroundColor: home, marginTop: 0, height: 1000 }}>Blog.</h1>
  </Layout>
)

export default Blog
