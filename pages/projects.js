import Layout from '../components/Layout'
import { home } from '../assets/colors'

const Projects = () => (
  <Layout>
    <h1 style={{ backgroundColor: home, marginTop: 0, height: 1000 }}>Projects.</h1>
  </Layout>
)

export default Projects
